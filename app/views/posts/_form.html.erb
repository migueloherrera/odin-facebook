<%= form_for(@post) do |f| %>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit "Publish post", class: "btn btn-primary"%>
  </div>
<% end %>
